package webdav.tree.impl;

import junit.framework.TestCase;

public class SimpleDirectoryTreeTest extends TestCase {

	/*
	 * SimpleDirectoryTree needs to be refactored so that we can test the methods without having a
	 * database running. An SimpleDirectoryTree.init-Method would do it.
	 */
	public void testGetTagNameFromPath() {
	// final SimpleDirectoryTree dirTree = new SimpleDirectoryTree("/root/path");
	// assertEquals("x-tag", dirTree.getTagNameFromPath("/files/x/x-tag/files/test.txt"));
	//
	// try {
	// dirTree.getTagNameFromPath("/a/path/test.txt");
	// fail("Should throw exception");
	// } catch (final UnsupportedOperationException ex) {}
	}
}

Für die Anfrage 

String showBookEntry = "SELECT " 
				+ "u.book_url, b.book_description, b.book_extended, b.content_id, g.group_name "
				+ "FROM bookmark b, urls u, groups g " 
				+ "WHERE u.book_url = ? "
				+ "  AND b.user_name = ? "
				+ "  AND (g.user_name = ? OR g.group IN (0,1,2) )"
				+ "  AND b.group = g.group "
				+ "  AND b.book_url_hash=u.book_url_hash";
				
aus BookmarkShowHandler ist evtl. noch ein Index mit user_name,book_url_hash (oder andersrum) sinnvoll.



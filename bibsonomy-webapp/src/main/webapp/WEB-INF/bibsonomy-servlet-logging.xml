<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:util="http://www.springframework.org/schema/util"
	xmlns:jee="http://www.springframework.org/schema/jee"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
						http://www.springframework.org/schema/beans/spring-beans.xsd
						http://www.springframework.org/schema/util 
						http://www.springframework.org/schema/util/spring-util-2.5.xsd
						http://www.springframework.org/schema/jee
						http://www.springframework.org/schema/jee/spring-jee-3.0.xsd">

	<!--+
		|
		| logging servlet config
		|
		+ -->
	<bean id="loggingUrlMapping" parent="urlMapping" class="org.springframework.web.servlet.handler.SimpleUrlHandlerMapping">
		<property name="urlMap">
			<map merge="true">
				<entry key="/logging/**">
					<bean class="org.bibsonomy.webapp.util.spring.controller.ServletWrappingController">
						<property name="servletInstance" ref="loggingServletInstance" />
						<property name="requireSession" value="false" />
					</bean>
				</entry>
			</map>
		</property>
	</bean>

	<bean id="loggingServletInstance" class="org.bibsonomy.logging.LoggingServlet">
		<property name="loggingDatabaseManager" ref="loggingManager" />
	</bean>
	
	<bean id="loggingManager" class="org.bibsonomy.logging.LoggingDatabaseManager">
		<property name="sessionFactory" ref="loggingSessionFactory" />
	</bean>
	
	<bean id="loggingSqlMapClient" class="org.springframework.orm.ibatis.SqlMapClientFactoryBean">
		<property name="configLocation" value="classpath:SqlMapConfigLogger.xml" />
		<property name="dataSource" ref="loggingDataSource" />
		<property name="transactionConfigClass" value="com.ibatis.sqlmap.engine.transaction.jdbc.JdbcTransactionConfig" />
	</bean>
	
	<bean id="loggingSessionFactory" class="org.bibsonomy.database.common.impl.SqlMapClientDBSessionFactory">
		<property name="client" ref="loggingSqlMapClient" />
	</bean>
	
	<bean id="loggingDataSource" class="org.apache.tomcat.dbcp.dbcp.BasicDataSource" parent="basicDataSource">
		<property name="driverClassName" value="${database.clicklogger.driverClassName}" />
		<property name="url" value="${database.clicklogger.url}" />
		<property name="username" value="${database.clicklogger.username}" />
		<property name="password" value="${database.clicklogger.password}" />
		<property name="maxIdle" value="${database.clicklogger.maxIdle}" />
		<property name="maxActive" value="${database.clicklogger.maxActive}" />
		<property name="maxWait" value="${database.clicklogger.maxWait}" />
		<property name="removeAbandoned" value="${database.clicklogger.removeAbandoned}" />
		<property name="removeAbandonedTimeout" value="${database.clicklogger.removeAbandonedTimeout}" />
	</bean>
</beans>
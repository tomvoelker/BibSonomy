<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:util="http://www.springframework.org/schema/util"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
	                    http://www.springframework.org/schema/beans/spring-beans.xsd
	                    http://www.springframework.org/schema/util 
	                    http://www.springframework.org/schema/util/spring-util-3.0.xsd
	                    http://www.springframework.org/schema/jee
	                    http://www.springframework.org/schema/jee/spring-jee-3.0.xsd">
	
	<!-- import the config from the lucene module -->
	<import resource="classpath:org/bibsonomy/bibsonomy-search-context.xml" />
	
	<bean id="luceneDataSource" class="org.apache.tomcat.dbcp.dbcp.BasicDataSource" parent="basicDataSource">
		<property name="driverClassName" value="${database.lucene.driverClassName}" />
		<property name="url" value="${database.lucene.url}" />
		<property name="username" value="${database.lucene.username}" />
		<property name="password" value="${database.lucene.password}" />
		<property name="maxIdle" value="${database.lucene.maxIdle}" />
		<property name="maxActive" value="${database.lucene.maxActive}" />
		<property name="maxWait" value="${database.lucene.maxWait}" />
		<property name="removeAbandoned" value="${database.lucene.removeAbandoned}" />
		<property name="removeAbandonedTimeout" value="${database.lucene.removeAbandonedTimeout}" />
	</bean>

	<!--+ 
	 	|
	 	| lucene CRON jobs are defined in bibsonomy2-servlet-cronjobs.xml
	 	|
	 	+-->
</beans>
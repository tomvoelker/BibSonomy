Module dependencies

  The dependencies are shown below:

[images/dependencies.png]

~~ The image can be generated with misc/scripts/findDependencies.sh

  An arrow denotes a dependency <from> a module <to> another module, e.g.
  rest-server depends on rest-common.